server:
  port: 8000

spring:
  datasource:
    username: root
    url: jdbc:mariadb://localhost:3306/spring_data
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update